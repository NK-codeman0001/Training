  <div>
  <h1> <%=@blog.title%></h2>
  <p><%=@blog.body%></p>
  </div>
  <div>
  <%= button_to "Edit", blog_path, method: :get %>
  <%= button_to "Delete", show_path, method: :delete, data: {turbo_confirm: "You want to delete Blog: #{@blog.title}"}%>
  <%= button_to "#{ @blog.archived ? "Unarchive" : "Archive"}", show_path, method: :patch%>
  </div>
